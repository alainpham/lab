tls:
  stores:
    default:
      defaultCertificate:
          keyFile: "/certs/cfg/live/${WILDCARD_DOMAIN}/privkey.pem"
          certFile: "/certs/cfg/live/${WILDCARD_DOMAIN}/fullchain.pem"

http:
  serversTransports:
    websocket:
      forwardingTimeouts:
        responseHeaderTimeout: 0s